name: Release

on:
  push:
    tags: ["v*.*.*"]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact: scriptmark
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            artifact: scriptmark
            cross: true
          - target: x86_64-apple-darwin
            os: macos-latest
            artifact: scriptmark
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact: scriptmark
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            artifact: scriptmark.exe

    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2

      - name: Install cross-compilation tools
        if: matrix.cross
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Build
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
        run: cargo build --release -p scriptmark-cli --target ${{ matrix.target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: scriptmark-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/${{ matrix.artifact }}

  wheels:
    name: Wheel ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist
          manylinux: auto
      - uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.target }}
          path: dist/*.whl

  publish-github:
    name: GitHub Release
    needs: [build]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Download binaries
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: scriptmark-*

      - name: Organize release assets
        run: |
          mkdir -p release
          cp artifacts/scriptmark-x86_64-unknown-linux-gnu/scriptmark release/scriptmark-linux-x86_64
          cp artifacts/scriptmark-aarch64-unknown-linux-gnu/scriptmark release/scriptmark-linux-aarch64
          cp artifacts/scriptmark-x86_64-apple-darwin/scriptmark release/scriptmark-macos-x86_64
          cp artifacts/scriptmark-aarch64-apple-darwin/scriptmark release/scriptmark-macos-aarch64
          cp artifacts/scriptmark-x86_64-pc-windows-msvc/scriptmark.exe release/scriptmark-windows-x86_64.exe
          chmod +x release/scriptmark-*

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: release/*

  publish-crates:
    name: Publish to crates.io
    needs: [build]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable

      - name: Publish crates
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: |
          for crate in scriptmark-core scriptmark-runner scriptmark-db scriptmark-canvas scriptmark-tui scriptmark-cli; do
            cargo publish -p "$crate" --no-verify
            sleep 30
          done

  publish-pypi:
    name: Publish to PyPI
    needs: [wheels]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    environment: release
    permissions:
      id-token: write

    steps:
      - name: Download wheels
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: wheel-*
          merge-multiple: true

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
